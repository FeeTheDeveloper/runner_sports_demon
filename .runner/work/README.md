# Work Claims

Before modifying shared architecture, create one JSON claim in this directory with `agent`, `repository`, `branch`, `paths`, `task`, `started_at`, and `status`. Claims should be removed or marked complete when the work is integrated. Never include secrets.
