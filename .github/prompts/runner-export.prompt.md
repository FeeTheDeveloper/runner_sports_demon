---
mode: agent
description: Validate or produce a curated Runner export.
---

Use versioned manifests, required artifacts, row counts, source coverage, timestamps, and checksums. Validate before handoff and never include secrets or unapproved raw provider data.
